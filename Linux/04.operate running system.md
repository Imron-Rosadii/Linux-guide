---
# **4️⃣ Mengoperasikan Sistem yang Berjalan (Operate Running Systems – RHEL Style)**

Bagian ini fokus pada **mengontrol sistem yang sedang aktif**,
mulai dari **power control, proses, hingga log**.
Ini **inti kerja SysAdmin harian**.
---

## **4.a Boot, Reboot, dan Shutdown**

### 📌 Penjelasan

- Mengontrol **siklus hidup sistem**
- Digunakan saat:

  - maintenance
  - update kernel
  - troubleshooting

- Perintah ini **butuh hak akses root**

---

### 📌 Shutdown Sistem

```bash
sudo poweroff
```

📌 Mematikan sistem **seketika**

---

### 📌 Reboot Sistem

```bash
sudo reboot
```

📌 Restart sistem **sekarang**

---

### 📌 Shutdown Terjadwal

```bash
sudo shutdown +15 "Sistem akan mati untuk maintenance dalam 15 menit."
```

📌 Sistem akan mati **15 menit lagi**
📌 Pesan akan dikirim ke user yang sedang login

---

### 📌 Batalkan Shutdown

```bash
sudo shutdown -c
```

---

## **4.b Mengelola Proses**

### 📌 Penjelasan

- Setiap program yang berjalan = **proses**
- SysAdmin harus bisa:

  - melihat proses
  - mencari PID
  - menghentikan proses
  - mengatur prioritas

---

### 📌 Melihat Proses

```bash
ps aux
```

📌 Snapshot semua proses

---

### 📌 Monitoring Interaktif

```bash
top
```

📌 Mirip **Task Manager** di Windows
📌 Tekan `q` untuk keluar

---

### 📌 Mencari PID Proses

```bash
pgrep -f httpd
```

📌 Menampilkan PID proses `httpd`

Alternatif:

```bash
ps aux | grep httpd
```

---

### 📌 Menghentikan Proses (kill)

```bash
kill -15 1234
```

📌 **SIGTERM (15)**
→ minta proses berhenti dengan baik (default)

---

```bash
kill -9 1234
```

📌 **SIGKILL (9)**
→ paksa berhenti (opsi terakhir)

⚠️ **SIGKILL bisa menyebabkan data corruption**

---

### 📌 Prioritas Proses (nice & renice)

- Rentang nice value: **-20 sampai 19**
- Semakin kecil → semakin tinggi prioritas

---

Menjalankan perintah dengan prioritas rendah:

```bash
nice -n 10 command_to_run
```

Mengubah prioritas proses yang sedang berjalan:

```bash
renice +5 1234
```

---

## **4.c Mengelola Log Sistem**

### 📌 Penjelasan

- Log = **alat utama troubleshooting**
- RHEL menggunakan:

  - **systemd-journald**

- Log bersifat:

  - terpusat
  - bisa difilter
  - bisa real-time

---

### 📌 Melihat Log Real-Time

```bash
journalctl -f
```

📌 Mirip `tail -f`

---

### 📌 Log Service Tertentu

```bash
journalctl -u httpd.service
```

📌 Fokus ke satu service

---

### 📌 Log Boot Terakhir

```bash
journalctl -b
```

---

### 📌 Filter Berdasarkan Level Error

```bash
journalctl -p err
```

Level umum:

- emerg
- alert
- crit
- err
- warning
- info
- debug

---

### 📌 Log Tradisional (/var/log)

```bash
ls /var/log/
```

Contoh penting:

- `/var/log/messages`
- `/var/log/secure`
- `/var/log/dnf.log`
- `/var/log/cron`

---

## **4.d Contoh Praktik SysAdmin**

### 📌 Cek Masalah Service Web

```bash
systemctl status httpd
journalctl -u httpd.service
```

---

### 📌 Cari Error Login SSH

```bash
journalctl -u sshd
grep ssh /var/log/secure
```

---
